
const Server = require('./server');
const Order = require('./order.model');
const Tienda = require('./store.model');
const Usuario = require('./user.model');

module.exports = {
    Server,
    Order,
    Usuario, 
    Tienda
}
